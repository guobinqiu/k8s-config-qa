apiVersion: apps/v1
kind: Deployment
metadata:
  name: target-tracking-kpi-ys
  namespace: kube-ops
spec:
  strategy:
    rollingUpdate:
      maxSurge: 0
      maxUnavailable: 1
    type: RollingUpdate
  replicas: 1
  selector:
    matchLabels:
      name: target-tracking-kpi-ys
  template:
    metadata:
      labels:
        name: target-tracking-kpi-ys
    spec:
      containers:
      - name: target-tracking-kpi-ys
        image: stagingacr.azurecr.cn/target-tracking-kpiengine:latest
        ports:
        - containerPort: 7879
        env:
        - name: TZ
          value: Asia/Shanghai
        - name: EVENTHUB_ENDPOINT
          value: "Endpoint=sb://ce2-cndgt-evh-np-cvccpd.servicebus.chinacloudapi.cn/;SharedAccessKeyName=authrule_cvc_lrl;SharedAccessKey=QsWjnGuq5MTtxc8HT7v9xTakQ/mza3OtUd4Gfb7gWKo=;EntityPath=cvc_lrl"
        volumeMounts:
        - name: data
          mountPath: /data
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: pvc-target-tracking-ys
---
apiVersion: v1
kind: Service
metadata:
  name: target-tracking-kpi-ys
  namespace: kube-ops
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 7879
    targetPort: 7879
  selector:
    name: target-tracking-kpi-ys
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-target-tracking-ys
  namespace: kube-ops
spec:
  accessModes:
  - ReadWriteOnce
  storageClassName: managed-premium
  resources:
    requests:
      storage: 100Gi
